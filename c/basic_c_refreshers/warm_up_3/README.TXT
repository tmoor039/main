THIS FOLDER IS ORGANIZED AS FOLLOWS:

The problem1 directory contains the source code used to compile the sample problem and also
screenshots proving it compiled on my unix enviroment. 

The problem2 directory contains a program that is made up of a 
 struct with a complex double member and its required functions.
To run, type make in the command line to compile and see the results of the functions. 

The problem3 directory contains a small program with the function asked.To run
just type make to compile and run the executable file.

The problem4 directory contains a text file with the function

The problem 5 directory contains a text file with the answer
  

